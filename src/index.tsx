// Legacy entry point - re-exports from react wrapper for backwards compatibility
export { MarkdownEditor, default } from './react';
export type { MarkdownEditorProps, ToolbarItem } from './core/types';
